import React from "react";

const Button = () => {
  return (
    <button className="text-sm font-bold text-white rounded-full py-3 px-8 bg-gradient-to-r to-cyan-500  from-emerald-400 ">
      Request Invite
    </button>
  );
};

export default Button;
